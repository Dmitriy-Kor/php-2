<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gallery</title>
</head>
<body>
    {% for item in items %}
    <div class="gallery__box">
        <a href="/?p=2&id={{item.id_img}}"><img class="gallery__img" src="{{item.adress}}{{item.name}}" alt="{{item.alt}}"/></a>
		<p class="gallery__text">просмотров: {{item.views}}</p>
    </div>
    {% endfor %}

</body>
</html>